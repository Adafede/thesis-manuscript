# Pandoc --defaults for LaTeX output.
# Load on top of common defaults.
to: latex
standalone: true
output-file: output/manuscript.tex
resource-path:
 - '.'
 - content
metadata:
  - lof: true
  - lot: true
toc: true